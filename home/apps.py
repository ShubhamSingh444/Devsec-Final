"""
This module contains the AppConfig class from\
      Django's apps module, which is used for configuring Django applications.
"""
from django.apps import AppConfig
class HomeConfig(AppConfig):
    """
    Configuration class for the 'home' app.
    """
    name = 'home'
